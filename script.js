const root=document.documentElement;const themeToggle=document.getElementById('themeToggle');const motionToggle=document.getElementById('motionToggle');themeToggle.addEventListener('click',()=>{root.dataset.theme=root.dataset.theme==='dark'?'light':'dark';themeToggle.textContent=root.dataset.theme==='dark'?'Dark':'Light'});motionToggle.addEventListener('click',()=>{document.body.classList.toggle('reduced-motion');motionToggle.textContent=document.body.classList.contains('reduced-motion')?'Off':'On'});
function tick(){const now=new Date();clock.textContent=now.toLocaleTimeString([],{hour:'2-digit',minute:'2-digit'});dateLine.textContent=now.toLocaleDateString([],{weekday:'short',month:'short',day:'numeric'});}tick();setInterval(tick,1000);
const sections=[...document.querySelectorAll('main section')];const navs=[...document.querySelectorAll('.nav a')];const io=new IntersectionObserver(entries=>{entries.forEach(e=>{if(e.isIntersecting){e.target.classList.add('visible');navs.forEach(a=>a.classList.toggle('active',a.hash==='#'+e.target.id));}})},{rootMargin:'-25% 0px -60% 0px'});sections.forEach(s=>io.observe(s));
const search=document.getElementById('search'),results=document.getElementById('searchResults');search.addEventListener('input',()=>{const q=search.value.toLowerCase().trim();results.innerHTML='';document.querySelectorAll('.highlight').forEach(x=>x.classList.remove('highlight'));if(!q)return;sections.filter(s=>(s.dataset.search||s.textContent).toLowerCase().includes(q)).slice(0,6).forEach(s=>{const a=document.createElement('a');a.href='#'+s.id;a.textContent=s.querySelector('h2,h1')?.textContent||s.id;a.onclick=()=>setTimeout(()=>{s.classList.add('highlight');setTimeout(()=>s.classList.remove('highlight'),1600)},260);results.appendChild(a)});if(!results.children.length)results.innerHTML='<span class="muted">No matches.</span>';});
